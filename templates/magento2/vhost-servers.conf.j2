#jinja2: lstrip_blocks: True
# {{ ansible_managed }}

server {
    listen {{ nginx_listen_port }} ;
    server_name {{ item.domain }};

    include sites-available/{{ item.vhost_include|default(magento2_vhost_default_include) }};
}